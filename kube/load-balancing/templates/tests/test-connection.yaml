apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "load-balancing.fullname" . }}-test-connection"
  labels:
    {{- include "load-balancing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "load-balancing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
